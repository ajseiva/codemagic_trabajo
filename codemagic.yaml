workflows:
  default-workflow:
    name: Docker Workflow
    environment:
      docker:
        image: "alexrodri7/codemagic-local:latest"  # Imagen Docker personalizada
    scripts:
      - name: Build Project
        script: mvn clean package
      - name: Run Tests
        script: mvn test
    artifacts:
      - target/*.jar  # Especifica los artefactos que se generarán
